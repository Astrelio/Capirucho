{
  "name": "el-capirucho",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "netlify dev",
    "build": "npm run build --prefix frontend",
    "test": "npm run test --prefix frontend && npm run test --prefix netlify",
    "typecheck": "tsc --noEmit -p netlify && npm run typecheck --prefix frontend"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "@netlify/functions": "^2.8.0",
    "@types/node": "^26.1.0",
    "typescript": "^5.5.0"
  }
}
